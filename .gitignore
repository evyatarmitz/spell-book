# Personal library — lives outside the repo
library/

# Tauri build output
src-tauri/target/

# Node (only needed if someone uses npm tooling locally)
node_modules/
npm-debug.log*

# OS junk
.DS_Store
Thumbs.db
Thumbs.db:encryptable

# Claude Code workspace files
.claude/
