# Personal library — lives outside the repo
library/

# Build output (workspace target dir)
/target/
src-tauri/target/

# Stray copies if manually placed at repo root for testing
/spell-book.exe
/sb.exe

# Node (only needed if someone uses npm tooling locally — not required to build)
node_modules/
npm-debug.log*
package-lock.json

# OS junk
.DS_Store
Thumbs.db
Thumbs.db:encryptable

# Claude Code workspace files
.claude/
