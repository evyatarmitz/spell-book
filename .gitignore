# Personal library — lives outside the repo
library/

# Tauri build output
src-tauri/target/

# Downloaded by start.bat when run in this folder
/spell-book.exe
/sb.exe

# Node (only needed if someone uses npm tooling locally — not required to build)
node_modules/
npm-debug.log*
package-lock.json

# OS junk
.DS_Store
Thumbs.db
Thumbs.db:encryptable

# Claude Code workspace files
.claude/
