{
  "name": "spell-book",
  "version": "1.0.0",
  "description": "Local-first personal code library — drop a source file, scavenge its functions, catalog them forever.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "sb": "./cli.js"
  },
  "scripts": {
    "start": "node server.js",
    "postinstall": "node scripts/copy-prism.js",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "archiver": "^6.0.1",
    "express": "^4.18.2",
    "prismjs": "^1.29.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
